<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="asusx.testtranslater.MainActivity">

    <Spinner
        android:id="@+id/FirstList"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"></Spinner>

    <EditText
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:inputType="textMultiLine"
        android:ems="8"
        android:layout_below="@id/FirstList"
        android:id="@+id/ToTranslate" />

    <Spinner
        android:id="@+id/SecondList"
        android:layout_alignBaseline="@id/FirstList"
        android:layout_toEndOf="@id/ToTranslate"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"></Spinner>

    <EditText
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_toRightOf="@id/ToTranslate"
        android:layout_alignBaseline="@id/ToTranslate"
        android:inputType="textMultiLine"
        android:ems="8"
        android:id="@+id/TranslateText" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/btn"
        android:layout_centerHorizontal="true"
        android:layout_below="@id/ToTranslate"
        android:text="@string/BtnText"
        android:onClick="StartTranslate"
        />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/history"
        android:layout_below="@id/btn"
        />
</RelativeLayout>
